const Loader = () => {
  return (
    <div className="fixed w-full h-full inset-0 backdrop-blur-md flex items-center justify-center">
      <div className="w-10 h-10 rounded-full bg-transparent border-l border-l-d-Background animate-spin"></div>
    </div>
  );
};

export default Loader;
